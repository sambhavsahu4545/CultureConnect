# ============================================
# Culture Connect Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# Command: cp env.example .env
# NEVER commit .env to git - it contains sensitive information

# ============================================
# REQUIRED - Core Configuration
# ============================================

# Node Environment (development | production)
NODE_ENV=production

# Server Port (Railway/Render will set this automatically, but you can override)
PORT=5000

# Server Host (usually 0.0.0.0 for cloud deployments, localhost for local)
HOST=0.0.0.0

# MongoDB Connection String
# Get this from MongoDB Atlas: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGODB_URI=your_mongodb_connection_string_here

# JWT Secret Key (REQUIRED for authentication)
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_minimum_32_characters

# JWT Token Expiration (optional, defaults to 7d)
JWT_EXPIRE=7d

# ============================================
# REQUIRED - CORS Configuration (Production)
# ============================================

# Allowed Origins for CORS (comma-separated)
# Add your frontend URLs here (e.g., https://your-app.vercel.app)
# For multiple origins: https://app1.vercel.app,https://app2.vercel.app
ALLOWED_ORIGINS=https://your-frontend-url.vercel.app

# ============================================
# OPTIONAL - Google Maps API (for location services)
# ============================================

# Google Maps API Key (optional, for location search and geocoding)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ============================================
# OPTIONAL - Email Service (for OTP via email)
# ============================================

# SMTP Configuration for sending emails
# Example for Gmail: smtp.gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here

# Email address to send from
FROM_EMAIL=noreply@cultureconnect.com

# ============================================
# OPTIONAL - SMS Service (for OTP via SMS)
# ============================================

# Twilio Configuration for sending SMS
# Get from: https://www.twilio.com/console
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# ============================================
# Notes:
# ============================================
# 1. For production deployment on Railway/Render:
#    - Set NODE_ENV=production
#    - Set MONGODB_URI to your MongoDB Atlas connection string
#    - Set JWT_SECRET to a secure random string
#    - Set ALLOWED_ORIGINS to your frontend URL(s)
#
# 2. For local development:
#    - Set NODE_ENV=development
#    - Use localhost MongoDB or MongoDB Atlas
#    - ALLOWED_ORIGINS can be omitted (localhost is allowed by default)
#
# 3. OTP Services (Email/SMS):
#    - Currently works in development mode (logs to console)
#    - For production, configure SMTP or Twilio credentials
#    - If not configured, OTP will still work but won't send real emails/SMS
#
# 4. Google Maps API:
#    - Optional but recommended for location services
#    - Without it, location search and geocoding won't work
#    - Free tier: $200 credit per month

